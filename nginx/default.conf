server {
 listen 80;
 server_name masjid-nour.org www.masjid-nour.org;

 location /.well-known/acme-challenge/ {
 root /var/www/certbot;
 }

 location / {
 root /usr/share/nginx/html;
 index index.html;
 try_files $uri $uri/ /index.html;
 }
}
